import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  output: "export",
  basePath: "/rapor-index", // Remove or change this if you are using a custom domain or a different repository name
};

export default nextConfig;
